#pragma once

/**
 * @brief An enum class that represents the valid Directions for a player.
 */
enum class Direction
{
    UP_LEFT,
    UP_RIGHT,
    UP,
    LEFT,
    RIGHT,
    DOWN_LEFT,
    DOWN_RIGHT,
    DOWN
};
